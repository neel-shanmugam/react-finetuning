Generate a React component for a simple button with a hover effect. The hover effect should change the color of the button from blue to red. Name the component HoverButton.

### Answer

```jsx
const HoverButton = () => {
  return (
    <button
      style={{
        backgroundColor: 'blue',
        color: 'white',
        padding: '16px 24px',
        fontSize: '16px',
        cursor: 'pointer',
      }}
      onMouseEnter={() => {
        document.body.style.backgroundColor = 'red';
      }}
      onMouseLeave={() => {
        document.body.style.backgroundColor = 'blue';
      }}
    >
      Hover Me
    </button>
  );
};
```

### Question 2

Generate a React component for a simple button with a hover effect. The hover effect should change the color of the button from blue to red. Name the component HoverButton.

### Answer

```jsx
const HoverButton = () => {
  return (
    <button
      style={{
        backgroundColor: 'blue',
        color: 'white',
        padding: '16px 24px',
        fontSize: '16px',
        cursor: 'pointer',
      }}
      onMouseEnter={() => {
        document.body.style.backgroundColor = 'red';
      }}
      onMouseLeave={() => {
        document.body.style.backgroundColor = 'blue';
      }}
    >
      Hover Me
    </button>
  );
};
```

### Question 3

Generate a React component for a simple button with a hover effect. The hover effect should change the color of the button from blue to red. Name the component HoverButton.

### Answer

```jsx
const HoverButton = () => {
  return (
    <button
      style={{
        backgroundColor: 'blue',
        color: 'white',
        padding: '16px 24px',
        fontSize: '16px',
        cursor: 'pointer',
      }}
      onMouseEnter={() => {
        document.body.style.backgroundColor = 'red';
      }}
      onMouseLeave={() => {
        document.body.style.backgroundColor = 'blue';
      }}
    >
      Hover Me
    </button>
  );
};
```

### Question 4

Generate a React component for a simple button with a hover effect. The hover effect should change the color of the button from blue to red. Name the component HoverButton.

### Answer

```jsx
const HoverButton = () => {
  return (
    <button
      style={{
        backgroundColor: 'blue',
        color: 'white',
        padding: '16px 24px',
        fontSize: '16px',
        cursor: 'pointer',
      }}
      onMouseEnter={() => {
        document.body.style.backgroundColor = 'red';
      }}
      onMouseLeave={() => {
        document.body.style.backgroundColor = 'blue';
      }}
    >
      Hover Me
    </button>
  );
};
```

### Question 5

Generate a React component for a simple button with a hover effect. The hover effect should change the color of the button from blue to red. Name the component HoverButton.

### Answer

```jsx
const HoverButton = () => {
  return (
    <button
      style={{
        backgroundColor: 'blue',
        color: 'white',
        padding: '16px 24px',
        fontSize: '16px',
        cursor: 'pointer',
      }}
      onMouseEnter={() => {
        document.body.style.backgroundColor = 'red';
      }}
      onMouseLeave={() => {
        document.body.style.backgroundColor = 'blue';
      }}
    >
      Hover Me
    </button>
  );
};
```

### Question 6

Generate a React component for a simple button with a hover effect. The hover effect should change the color of the button from blue to red. Name the component HoverButton.

### Answer

```jsx
const