Build a React component for a modal dialog box that can be opened or closed with a button. Name the component ModalDialog.

The component should have the following properties:

- `isOpen`: A boolean value that determines whether the modal dialog box is open or not.
- `onClose`: A function that is called when the user clicks the close button or presses the Escape key.
- `children`: The content of the modal dialog box.

The component should have the following methods:

- `open`: Opens the modal dialog box.
- `close`: Closes the modal dialog box.

The component should have the following styles:

- `modal-dialog`: A CSS class that styles the modal dialog box.
- `modal-dialog-open`: A CSS class that styles the modal dialog box when it is open.
- `modal-dialog-close`: A CSS class that styles the modal dialog box when it is closed.

The component should have the following events:

- `onClick`: A function that is called when the user clicks the close button or presses the Escape key.

The component should have the following dependencies:

- `React`: A JavaScript library for building user interfaces.
- `ReactDOM`: A JavaScript library for rendering React components in the browser.

The component should have the following dependencies:

- `React`: A JavaScript library for building user interfaces.
- `ReactDOM`: A JavaScript library for rendering React components in the browser.

The component should have the following dependencies:

- `React`: A JavaScript library for building user interfaces.
- `ReactDOM`: A JavaScript library for rendering React components in the browser.

The component should have the following dependencies:

- `React`: A JavaScript library for building user interfaces.
- `ReactDOM`: A JavaScript library for rendering React components in the browser.

The component should have the following dependencies:

- `React`: A JavaScript library for building user interfaces.
- `ReactDOM`: A JavaScript library for rendering React components in the browser.

The component should have the following dependencies:

- `React`: A JavaScript library for building user interfaces.
- `ReactDOM`: A JavaScript library for rendering React components in the browser.

The component should have the following dependencies:

- `React`: A JavaScript library for building user interfaces.
- `ReactDOM`: A JavaScript library for rendering React components in the browser.

The component should have the following dependencies:

- `React`: A JavaScript library for building user interfaces.
- `ReactDOM`: A JavaScript library for rendering React components in the browser.

The component should have the following dependencies:

- `React`: A JavaScript library for building user interfaces.
- `ReactDOM`: A JavaScript library for rendering React components in the browser.

The component should have the following dependencies:

- `React`: A JavaScript library for building user interfaces.
- `ReactDOM`: A JavaScript library for rendering React components in the browser.

The component should have the following dependencies:

- `React`: A JavaScript library for building user interfaces.
- `ReactDOM`: A JavaScript library for rendering React components in the browser.

The component should have the following dependencies:

- `React`: A JavaScript library for building user interfaces.
- `ReactDOM`: A JavaScript library for rendering React components in the browser.

The component should have the following dependencies:

- `React`: A JavaScript library for building user interfaces.
- `ReactDOM`: A JavaScript library for rendering React components in the browser.

The component should have the following dependencies:

- `React`: A JavaScript library for building user interfaces.
- `ReactDOM`: A JavaScript library for rendering React components in the browser.

The component should have the following dependencies:

- `React`: A JavaScript library for building user interfaces.
- `ReactDOM`: A JavaScript library for rendering React components in the browser.

The component should have the following dependencies:

- `React`: A JavaScript library for building user interfaces.
- `ReactDOM`: A JavaScript library for rendering React components in the browser.

The component should have the following dependencies:

- `React`: A JavaScript library for building user interfaces.
- `ReactDOM`: A JavaScript library for rendering React components in the browser.

The component